# Android build artifacts
*.iml
.gradle/
local.properties
.idea/
build/
captures/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class

# Kotlin
.kotlin/

# Generated
**/generated/

# Secrets (never commit these)
google-services.json
*.keystore
keystore.properties
# Exception: the shared DEBUG keystore is intentionally committed so every
# build is signed identically (install-over without uninstall). Debug
# keystores are not secret. Release keystores must still never be committed.
!app/covault-debug.keystore
