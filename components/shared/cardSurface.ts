// Both general cards and Review cards use the same surface. Keep the colors
// selected by each card separate: their red variants currently mean different
// things, while the base surface is identical.
export const CARD_SURFACE_CLASSES =
  'bg-white dark:bg-slate-900 rounded-[2.5rem] p-6 shadow-xl border ring-1 ring-inset ring-white/10 dark:ring-white/[0.04]';
