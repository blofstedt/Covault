# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
www
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
vercel.env.txt
node_modules
.auth-session.json
screenshots/

# Session-specific context file with credentials. Lives only on the
# developer's local machine, never committed.
CONTEXT.md
CONTEXT.local.md
*credentials*
*secrets*

# Generated by `npx cap add android` in CI (which does `rm -rf android` first).
# The real source for custom native code is `android-custom/`, copied in by
# scripts/sync-android.sh. Tracked copies here only go stale and mislead.
android/
